# `src/app/api/` — 伺服器端 API Routes（民眾端）

| 路由 | 功能 |
|---|---|
| `gemini/` | Gemini 代理：OCR、症狀分析、翻譯、整體建議。**金鑰只在伺服器端**，前端傳「結構化判定結果」而非原始數值 |
| `debug/` | 開發用診斷端點 |

隱私設計：Gemini 收到的是「正常/偏高/偏低」等判定，不是原始檢驗值 → 降低幻覺與隱私風險。
